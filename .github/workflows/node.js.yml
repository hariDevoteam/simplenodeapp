name: build
on: push
jobs:
  build:
    name: my job 
    runs-on : ${{matrix.os}}
    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [12 , 14]
    steps:
      - name: My first step
        env:
          MY_VAR: Hi there! My name is
          FIRST_NAME: Hari
          MIDDLE_NAME: Bahadur
          LAST_NAME: Khadka
        run: echo $MY_VAR $FIRST_NAME $MIDDLE_NAME $LAST_NAME

